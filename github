repo: shaya99stern-hash/NaviOS-hub
branch: main

## Last sync

date: 2026-08-12T00:00:00Z

The repository is currently empty — this project is the source of truth for it.
Nothing has been imported; the code below was authored here and is ready to be
pushed as the repo's initial commit.

### Updated in this project

- Rebuilt the companion agent as two processes: a Node transport layer and a
  Python device bridge on pymobiledevice3.
- Added the full iMazing capability set plus live process monitoring, on-device
  packet capture, configuration-profile auditing and removal.
- Added backup extraction that reads messages, contacts, calls, Safari history
  and photos straight out of Manifest.db.
- Implemented Apple’s encrypted-backup keybag from scratch, so Keychain, Health
  and call history are readable — the data unencrypted backups drop.

## Screen map

| Project screen | Repo files |
| --- | --- |
| NaviOS Hub App.dc.html | _(authored here — not yet in repo)_ |
| Agent transport | agent/navios-agent.js |
| Device bridge | agent/navios_bridge.py |
| Encrypted-backup engine | agent/navios_crypto.py |
| Agent packaging | agent/package.json, agent/requirements.txt |
| Agent setup docs | agent/README.md |
