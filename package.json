{
  "name": "navios-hub-agent",
  "version": "2.0.0",
  "description": "NaviOS Hub companion agent — iOS device toolkit over usbmux",
  "type": "module",
  "main": "navios-agent.js",
  "engines": { "node": ">=18" },
  "scripts": {
    "start": "node navios-agent.js",
    "setup": "pip install -r requirements.txt && npm install",
    "tunnel": "sudo pymobiledevice3 remote tunneld"
  },
  "dependencies": {
    "bonjour-service": "^1.2.1",
    "web-push": "^3.6.7",
    "ws": "^8.18.0"
  }
}
